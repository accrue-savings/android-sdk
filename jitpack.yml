before_install:
  - sdk install java 17.0.7-open
  - sdk use java 17.0.7-open

jdk:
  -openjdk17

install:
  - ./gradlew :EmbedSDK:assembleRelease
  - ./gradlew :EmbedSDK:publishToMavenLocal --stacktrace